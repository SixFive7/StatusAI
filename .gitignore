# build output
bin/
obj/
publish/
Releases/
*.pdb

# scratch: test renders, builds, downloads, anything throwaway
.work/

# local state: a token cache a stray offline render writes (StatusAI/tokens/<sid>.bin), IDE files
*.bin
*.bin.tmp
*.user
.vs/
