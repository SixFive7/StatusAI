# build output
bin/
obj/
publish/
Releases/
*.pdb

# scratch: test renders, builds, downloads, anything throwaway
.work/

# local state: a token cache a stray render writes (statusline-tokens/<sid>.bin), IDE files
*.bin
*.bin.tmp
*.user
.vs/
